/** @type {import('next').NextConfig} */
const nextConfig = {
  // Firebase App Hosting supports full Next.js features natively
  // No special output configuration needed
};

export default nextConfig;

